import numpy as np
from sklearn.metrics import euclidean_distance


def filter(image, hr):
    """
    Filters an image using hs, spatial range filter, hr, color range filter.

    params
    -------
        hs: spatial range filter
        hr: color range filter
    """
    # first compute the spatial range filter
    pass


def query_radius(X, h):
    """
    Query neighbours in the ball h
    """
    count = X.shape[-1]
    ind = X.shape[-1]
    dist = X.shape[-1]


if __name__ == "__main__":
    pass

